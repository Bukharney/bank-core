meta {
  name: Get Account Preview
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/account/preview/{{account_id}}
  body: none
  auth: none
}

tests {
  test("should return public account preview", function() {
    expect([200, 404]).to.include(res.getStatus());
  });
}
