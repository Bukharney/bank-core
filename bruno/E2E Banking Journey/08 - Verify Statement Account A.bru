meta {
  name: 08 - Verify Statement Account A
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/ledger/statement/{{account_a_id}}?limit=20&offset=0
  body: none
  auth: none
}

params:query {
  limit: 20
  offset: 0
}

headers {
  ~Cookie: access_token={{access_token}}
}

tests {
  test("should return ledger statement for Account A", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.be.an('array');
    expect(res.getBody().length).to.be.greaterThan(0);
  });
}
