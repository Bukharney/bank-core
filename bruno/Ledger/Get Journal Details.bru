meta {
  name: Get Journal Details
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/ledger/journal/{{journal_id}}
  body: none
  auth: none
}

headers {
  ~Cookie: access_token={{access_token}}
}

tests {
  test("should return journal details by ID", function() {
    expect([200, 400, 404]).to.include(res.getStatus());
  });
}
