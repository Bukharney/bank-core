# Agent Guidelines & Behavioral Rules

## Git & Deployment Rules

- **Do Not Push Without Explicit Command**: Never execute `git push` or push commits/tags to remote repositories automatically. Always wait for the user's explicit command or confirmation before pushing.
