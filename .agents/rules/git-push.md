---
trigger: always_on
---

# Git Push Rule

Never run `git push` or push changes to any remote repository without explicit user instructions or command. You may stage files (`git add`) and make local commits (`git commit`) when instructed to complete a task, but you must strictly wait for an explicit user command before pushing to `origin` or any remote.
